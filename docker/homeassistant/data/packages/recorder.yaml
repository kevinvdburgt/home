recorder:
  db_url: !secret recorder_db_url
  purge_keep_days: 365
  exclude:
    entity_globs:
      - sensor.*_ap_uptime
      - sensor.*_switch_uptime
    entities:
      - binary_sensor.water_meter_pulse_sensor
